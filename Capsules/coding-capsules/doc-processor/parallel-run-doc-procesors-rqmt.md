# Purposes
The doc process pipeline is very slow. Need to run multiple pipelines in paralle.

- MAX_DOC_PROCESS_PIPELINES defines the maximum number of pipelines to run in parallel
- Each pipeline runs in a separate thread
- Write the spec, implementation and test documents before finish this task
